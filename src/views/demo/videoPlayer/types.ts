export interface PlayerOptions {
    src: string
    autoplay: boolean
    preload: boolean
    loop: boolean
    height: string
    width: string
    controls: boolean
    muted: boolean
    poster: string
}